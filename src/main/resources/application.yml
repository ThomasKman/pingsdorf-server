spring:
  application:
    name: pingsdorf-server
  data:
    mongodb:
      uri: ${MONGODB_URI:mongodb://localhost:27017/pingsdorf}

server:
  port: 8080

management:
  endpoints:
    web:
      exposure:
        include: health, info
  endpoint:
    health:
      show-details: when-authorized
